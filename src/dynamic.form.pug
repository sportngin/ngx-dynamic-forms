doctype html

form([formGroup]='form', autocomplete='off', (ngSubmit)='submit($event)', novalidate)

    ng-template(ngFor let-control [ngForOf]='modelControls')
        element-selector([control]='control', [form]='form')
