doctype html

select.form-control(
    [formControl]='formControl',
    autocomplete='off',
    [ngClass]='{ "form-control-success": validatorSuccess, "form-control-danger": validatorError }',
    [attr.disabled]='(control.disabled || pendingPrerequisites) ? "" : null'
)
    option(
        *ngFor='let item of data',
        [ngValue]='itemValue ? item[itemValue] : item'
    ) {{item[itemLabel]}}