doctype html

input.form-control(
    [type]='type',
    [formControl]='input',
    [attr.disabled]='control.disabled ? "" : null',
    [ngClass]='{ "form-control-success": input.validator && input.valid, "form-control-danger": input.validator && !input.valid && input.touched }'
)